npm install vue-router@4
npm install -D sass-embedded

# 將 src/popup.ts 做取代修改
# createApp(Popup).mount('body')

# const app = createApp(Popup)
# app.mount('body')

# 插入，且需添加 route 目錄，檔案
# import router from './router'
# app.use(router)
